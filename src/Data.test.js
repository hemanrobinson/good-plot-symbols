import Data from './Data';

it( "returns column names", () => {
    expect( Data.getColumnNames()).toBe( undefined );
});

it( "returns values", () => {
    expect( Data.getValues()).toBe( undefined );
});

